<Window x:Class="YangzaiWorkshop.Views.TextHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="文本历史" Width="460" Height="560"
        MinWidth="360" MinHeight="400"
        WindowStartupLocation="CenterScreen"
        Topmost="True" ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}"
        FontFamily="Microsoft YaHei UI"
        ResizeMode="CanResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="&#xE81C;" FontFamily="Segoe MDL2 Assets" FontSize="18"
                       Foreground="{DynamicResource PrimaryBrush}" VerticalAlignment="Center"
                       Margin="0,0,8,0"/>
            <TextBlock Text="文本历史" FontSize="16" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- 说明 + 撤销/重做快捷操作 -->
        <Border Grid.Row="1" Background="{DynamicResource CardBackgroundBrush}"
                CornerRadius="8" Padding="12,10" Margin="0,0,0,12"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <StackPanel>
                <TextBlock x:Name="HintText" FontSize="11"
                           Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"
                           LineHeight="18"/>
                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="↶ 撤销" FontSize="12" Padding="12,5" Margin="0,0,8,0"
                            Cursor="Hand" Click="Undo_Click"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                    <Button Content="↷ 重做" FontSize="12" Padding="12,5" Margin="0,0,8,0"
                            Cursor="Hand" Click="Redo_Click"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                    <Button Content="清空历史" FontSize="12" Padding="12,5"
                            Cursor="Hand" Click="Clear_Click"
                            Style="{StaticResource SecondaryButtonStyle}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 历史快照列表 -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="SnapshotList"/>
        </ScrollViewer>

        <!-- 底部 -->
        <StackPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock x:Name="CountText" FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</Window>
